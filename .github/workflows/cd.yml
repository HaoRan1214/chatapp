name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: xvh1022
          password: Docker123

      - name: Build and push Docker images
        run: |
          docker-compose build
          docker-compose push

      - name: Deploy to server
        run: |
          ssh user@your_server_ip "docker-compose -f F:\ZZULI\23-24-2\SAI\Individual Assignment\chat app\docker-compose.yml pull && docker-compose -f F:\ZZULI\23-24-2\SAI\Individual Assignment\chat app\to/docker-compose.yml up -d"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
